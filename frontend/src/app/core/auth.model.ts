/**
 * Represents an authenticated user in the ReqTrace system.
 */
export interface User {
  /** The unique username for the user. */
  username: string;
  /** The display name of the user. */
  displayName: string;
}
